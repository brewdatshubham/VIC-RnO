# Copy this file to `secrets.toml` and fill values.
# IMPORTANT: Do NOT commit `secrets.toml` if this becomes a git repo.

[databricks]
host = "adb-.azuredatabricks.net"
http_path = "/sql/1.0/warehouses/"
token = "REPLACE_WITH_YOUR_TOKEN"

